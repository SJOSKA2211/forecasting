# Core data manipulation and analysis
pandas>=1.5.0,<2.0.0
numpy>=1.26.0,<2.0.0

# Machine learning
scikit-learn>=1.2.0

# Time series forecasting
statsmodels>=0.14.0
pmdarima>=2.0.3

# Deep learning
tensorflow==2.15.0 # Consider tensorflow-gpu if you have an NVIDIA GPU and CUDA setup

# Visualization and Streamlit GUI
streamlit>=1.30.0
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.18.0
# Need to install a backend for plotly.io to save images
# For static images (PNG, JPEG, etc.), install kaleido
kaleido>=0.2.1 # Added Kaleido for saving Plotly images
# If you need interactive HTML exports, you don't need kaleido, but for static images it's necessary.

altair>=5.2.0 # Optional: Add if you plan to use Altair visualizations

# Utilities
joblib>=1.2.0

# Additional dependencies that might be needed
scipy>=1.10.0
openpyxl>=3.1.0 # Added openpyxl for reading .xlsx files